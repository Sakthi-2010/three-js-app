import { motion, AnimatePresence } from "framer-motion";
import { useSnapshot } from "voltio";
import {
  headContainerAnimation,
  headContentAnimation,
  headTextAnimation,
  slideAnimation,
} from "../config/motion";
const Home = () => {
  return (
    <div>
      <h1>Home world world</h1>
    </div>
  );
};

export default Home;
